import { DisplayPropertyNamePipe } from './display-property-name.pipe';

describe('DisplayPropertyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPropertyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
